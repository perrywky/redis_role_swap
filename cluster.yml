floating_ip: 10.1.1.1
floating_ip_cidr: /24
interface: eth0
ssh_user: root # defaults: root
ssh_identify_file: NULL # defaults: do not use

instance_one:
    host: 192.168.1.1
    port: 6379
    auth: password_example

instance_two:
    host: 192.168.1.2
    port: 6379
    auth: password_example
